[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gaze-host-sdk"
version = "0.1.0"
description = "Host-side Gaze SDK protocol codec for Python"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
